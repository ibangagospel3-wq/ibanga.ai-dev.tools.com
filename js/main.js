document.addEventListener('DOMContentLoaded',()=>{$$('.faq details').forEach(item=>item.addEventListener('toggle',()=>{if(item.open)$$('.faq details').filter(other=>other!==item).forEach(other=>other.open=false)}));$$('[data-search]').forEach(input=>{input.addEventListener('input',()=>{const query=input.value.toLowerCase();const scope=input.dataset.search||'[data-search-item]';const items=$$(scope);let count=0;items.forEach(item=>{const match=item.textContent.toLowerCase().includes(query);item.hidden=!match;if(match)count++});const empty=$(input.dataset.empty||'.search-empty');if(empty)empty.hidden=count!==0})});});